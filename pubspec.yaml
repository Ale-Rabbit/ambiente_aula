name: nubank
description: Ambiente Aula.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  material_design_icons_flutter: ^4.0.5955
  flutter_statusbarcolor: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
